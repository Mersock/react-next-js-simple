import Layout from '../components/Layout';

const index = () => {
    return (
        <Layout>
            <div>
                Hello NEXT JS.
            </div>
        </Layout>
    );
};

export default index;
